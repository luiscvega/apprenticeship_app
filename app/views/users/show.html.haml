%h1 Welcome #{current_user.full_name}!

= render 'mentor_mode'

%hr

%h2 Your New Messages

%table
  - current_user.unread_messages.select("DISTINCT (user_id)").each do |message|
    %tr
      %td
        = current_user.unread_messages.where(user_id: message.sender.id).count
        from #{message.sender.full_name}
/%table
/ - current_user.unread_messages.each do |message|
/  %tr
/   %td= "#{current_user.unread_messages.where(user_id: message.sender.id).count} messages from #{message.sender.full_name}"
      
      
/ %p Full Name: #{current_user.full_name}
/ %p Email: #{current_user.email}
/ %p Description: #{current_user.description}
/ 
/ = link_to "Edit Account", edit_user_url(current_user)
