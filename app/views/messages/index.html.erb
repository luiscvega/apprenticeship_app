<h1>Messages</h1>

<table><%= render @messages %></table>

<%= form_for @message, :url => apprenticeship_messages_url do |f|%>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text, :rows => 3 %>
  </div>
  <div class="action">
    <%= f.submit "Send" %>
  </div>
<% end %>