<h1>Conversation</h1>

<%= render @conversations %>

<%= form_for @conversation do |f|%>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message, :rows => 3 %>
  </div>
  <div class="action">
    <%= f.submit :message %>
  </div>
<% end %>